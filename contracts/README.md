# contracts/

Stage 3 — Solidity implementing the frozen interfaces

This directory is created by Stage 0 and filled by the stage named above.
See `docs/ERC-8415-Native-Infrastructure-Kit-PRD-Stage-Delivery-v2.0.md`.

`examples/` holds minimal CC0 consumers of a projection. Nothing here depends
on them; they exist so the read-and-act boundary the Kit recommends is
executable rather than described. See `examples/README.md`.
